<view class="page">
  <view class="card guest-card" wx:if="{{guestMode}}">
    <view class="guest-title">通知中心</view>
    <view class="guest-desc muted">接收预约审批、动物状态与实验相关提醒。登录后可查看个人通知。</view>
    <button class="btn-primary" bindtap="goLogin">登录后查看</button>
  </view>

  <block wx:else>
  <view class="toolbar card">
    <view class="tip-text">轻触通知可标记已读</view>
    <button size="mini" class="btn-ghost" bindtap="markAll">全部已读</button>
  </view>

  <view wx:if="{{list.length === 0}}" class="empty">暂无通知</view>

  <view
    class="card notice {{item.read ? 'read' : 'unread'}}"
    wx:for="{{list}}"
    wx:key="id"
    data-id="{{item.id}}"
    bindtap="openNotice"
  >
    <view class="dot" wx:if="{{!item.read}}"></view>
    <view class="title">{{item.title}}</view>
    <view class="msg">{{item.message}}</view>
    <view class="muted time">{{item.timeText}}</view>
  </view>
  </block>
</view>
